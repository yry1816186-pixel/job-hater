# 贡献指南

感谢关注 Campus-Job-Agent。这份文档说明如何参与贡献，以及哪些改动会被接受、哪些不会被接受。

## 开始之前

- 本项目 **MIT** 协议；你提交的贡献默认以同等协议授权。
- 提交 Issue 前请先搜索现有 Issue，避免重复。
- 涉及招聘平台自动化的一切讨论，请先读下方「红线」。

## 红线（违反直接关闭，不做讨论）

1. **禁止编造数据**：不得提交任何把虚构岗位/虚构统计/占位演示数据当成真实数据展示的代码或文档。演示数据只能进 `tests/fixtures/`。
2. **禁止云端上传用户数据**：任何把 `data/profile/`、`data/out/` 内容发送到第三方服务器的代码路径都不会被合并。
3. **禁止硬刚反爬**：新采集方案必须是「官方 API > 结构化数据源 > 成熟工具封装 > 浏览器自动化」链条中的一环；需要破解验证码、签名算法或绕过登录墙的 PR 不会被合并。抓不到的平台，如实标注降级路径。
4. **禁止伪造验证状态**：适配器文档里的 ✅/⚠️/🚫 必须与实际验证一致。没跑通就写没跑通。

## 开发约定

- **core/ 只用 Python 标准库**（≥3.10）。第三方依赖只允许出现在 `adapters/`（独立 venv）与开发工具中。
- **core/ 不含任何个人事实**：用户相关的映射（如经历→证据ID）一律放画像数据（`profile.experience_ev_map`），代码保持对任意用户成立。
- **测试是自运行脚本**：`python3 tests/test_<topic>.py`，全部通过时输出 `ALL TESTS PASSED`。新功能必须带测试；修复 bug 必须先加一个能复现该 bug 的用例。
- 提交信息用中文或英文均可，一行主题说清"改了什么、为什么"；不要求行尾署名。
- 每次提交前本地跑完全部测试：

```bash
for t in tests/test_*.py; do python3 "$t" | tail -1; done
```

## 提交流程

1. Fork / 建分支（`git switch -c feat/xxx`）；
2. 改动 + 测试；
3. 跑测试全绿；
4. 发 PR，描述里写清：动机、改动点、验证方式（跑过哪些命令、真实输出摘要）；
5. 维护者评审后合并（见 docs/GOVERNANCE.md 的决策规则）。

## 报告缺陷

用 Issue 模板。涉及投递风控（风控闸门绕过、黑名单失效）或真实性校验（factcheck 漏洞）的缺陷，请在标题加 `[security]` 前缀并按 SECURITY.md 的流程私下报告。
