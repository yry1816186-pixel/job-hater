# 额外引入清单 — 自主调研成果（70项；存在性与内容当日实测核验，star 数为参考值）

> 调研方法：10 个方向并行调研（多agent），每项经 git clone / GitHub API / 直接HTTP抓取
> 三重验证，`verified_exists=false` 的一律未收录。核验日期：2026-09-20。
> 图例：🟢 直接集成（作为依赖/数据源用）｜🟡 提取模块（摘取代码或配置）｜🔵 参考思路（只借鉴设计）

## 一、本次部署已实际落地/配置的引入（优先级最高）

| 项目 | 方式 | 状态 |
|------|------|------|
| **OpenHire**（gzchenhao/openhire，PyPI openhire 0.6.0） | 🟢 直接集成 | ✅ pip 安装成功，`ohp serve` MCP 握手验证通过，已写入 Claude Code MCP 配置 |
| **mcp-jobs**（mergedao/mcp-jobs，npm v1.4.0） | 🟢 直接集成（配置层） | ⚠️ 包真实存在但当前版本实测不可用（缺bin/启动阻塞/爬取配置过期）；已用自研 wrap_stdio.js 卫生包装并写入 MCP 配置，待上游修复即用 |
| **boss-agent-cli**（can4hou6joeng4/boss-agent-cli） | 🟢 直接集成 | ✅ v2.0.0 uv 安装完成、`boss-mcp`（77工具）握手验证并写入 `.mcp.json`；`boss doctor` 结构化自检通过（网络OK，登录态需本地扫码） |
| **career-ops**（career-ops-hq/career-ops，MIT） | 🟡 提取模式 | ✅ 完整归档1438文件；modes/技能已提取到 skills/upstream/career-ops/；工作流框架作为六命令设计蓝本 |
| **ai-job-search**（MadsLorentzen/ai-job-search，MIT） | 🟡 提取模块 | ✅ 双Agent工作流/事实落地审计/闸门先于评分/JD反注入已吸收；命令脚本已归档 skills/upstream/ai-job-search/ |
| **本地 MCP 自研**（mcp/job_agent_mcp.py，7工具） | 自研 | ✅ stdio 零依赖，握手+工具调用全验证 |

## 二、各方向完整调研清单

### Boss直聘生态

#### 🟢 Ocyss/boss-helper
- 地址：https://github.com/Ocyss/boss-helper（⭐2238）｜TypeScript
- 核心能力：Boss直聘浏览器插件（WXT+Vue3，MIT）：批量投递简历、高级筛选（薪资/公司名/距离/公司风控/AI筛选）、GPT自动打招呼、企业黑名单、多账号管理，全部运行在用户已登录的真实浏览器里。
- 引入理由：求职者端Boss工具中star最高（2.2k+，2024创建持续维护至2026-08）。其『筛选条件→批量投递→招呼语模板→黑名单去重』这套投递链路与我们/apply命令、投递黑名单、每日限额需求一一对应；以扩展形式复用真实登录态，天然绕过登录与大部分反爬，无需额外cookie工程。
- 融合方式：直接集成
- ⚠️ 限制：需要图形界面浏览器并人工扫码登录一次；本质是浏览器扩展而非CLI，Claude Code只能管理其配置/规则，投递动作需在有头环境执行；自动投递存在账号风控风险（README自身有风险提示）。

#### 🟢 jackwener/boss-cli
- 地址：https://github.com/jackwener/boss-cli（⭐968）｜Python
- 核心能力：基于逆向API的Boss直聘CLI（PyPI: kabi-boss-cli）：关键词+城市/薪资/学历/经验/公司规模/融资阶段筛选搜索、个性化推荐流、投递记录/面试邀请/浏览历史查询、单发与批量打招呼（内置1.5秒限速与--dry-run）、CSV/JSON导出、40+城市。
- 引入理由：候选中唯一『无浏览器』方案：走API而非页面渲染，速度与稳定性远超浏览器自动化。cookie登录方案最完整——从10+种浏览器自动提取cookie、二维码登录兜底、用真实搜索API校验登录态有效性，正好补齐我们的cookie登录模块；输出{ok, schema_version, data}结构化JSON信封，与Claude Code工具链天然契合，可直接替代/补强boss-agent-cli的岗位数据源角色（含boss detail/securityId全量JD、批量greet）。
- 融合方式：直接集成
- ⚠️ 限制：GitHub未检测到LICENSE文件（默认版权保留，建议以pip安装后子进程调用、勿拷贝源码再分发）；逆向API会随Boss改版失效，必须设计降级路径；2026-04后更新放缓。

#### 🟡 yangfeng20/ai-job
- 地址：https://github.com/yangfeng20/ai-job（⭐680）｜Java
- 核心能力：『AI工作猎手』：vite-plugin-monkey油猴UI脚本 + Spring Boot 3后端（Spring AI接DeepSeek/GPT多模型池）。AI坐席7x24以求职者口吻自动回复HR、按场景匹配预设问答、自动发简历/交换联系方式、HR拒绝后自动挽留、高意向岗位邮件通知；自研WebSocket Hook解析Boss的Protobuf实时通讯协议。MIT。
- 引入理由：提供全生态少有的『沟通后半场』能力——不止打招呼，而是多轮对话自动化+拒绝挽留+意向度识别，直接对应我们/interview与投递状态追踪需求；其Protobuf协议逆向是理解Boss聊天通道的稀有资料；官方云服务已关停、官方明确转向本地部署，符合全本地化铁律。
- 融合方式：提取模块
- ⚠️ 限制：架构偏重（需本地起Spring Boot+MySQL），非开箱CLI；部分在线功能依赖自家服务器（已停），需自行部署后端并配LLM key；油猴脚本需浏览器开发者模式；同作者旧项目boss_batch_push已声明未适配Boss新版职位页，应以ai-job为准。

#### 🟡 longsizhuo/BossZhiPin_Job_Search
- 地址：https://github.com/longsizhuo/BossZhiPin_Job_Search（⭐192）｜Python
- 核心能力：Boss直聘自动打招呼：LLM按JD+简历生成个性化招呼语→规则审核→DRY_RUN人工确认后发送；浏览器自动化已从Selenium迁移到nodriver（更稳的过反爬路线），独立chrome_profile目录持久化cookie（扫码一次后续免登录）；支持任意OpenAI兼容端点（DeepSeek/GLM/本地Ollama）；自带.claude/skills/boss-zhipin-onboarding新手引导skill与pytauri桌面壳。MIT。
- 引入理由：『生成→规则审核→dry-run→发送』两段式话术流水线正是提示词3.4节打招呼话术模块想要的形态，且强调发送前人工审阅、不商业化，契合『事实不可捏造』原则；nodriver反检测经验+独立profile cookie方案可直接移植；2026-08仍在更新，文档最完整（甚至内置面向AI agent的引导skill）。
- 融合方式：提取模块
- ⚠️ 限制：仅支持Chrome/Chromium（nodriver不支持Edge/Safari）；需Python>=3.11与uv；nodriver为非官方逆向库，随Chrome版本升级可能抖动；原作者停更、社区接手维护。

#### 🟡 xiarongwen/boss-auto-job
- 地址：https://github.com/xiarongwen/boss-auto-job（⭐24）｜Python
- 核心能力：基于Camoufox（C++级指纹伪装Firefox）的Boss直聘全流程：搜索→AI简历匹配打分→JD差距分析→简历优化(PDF/Word)→个性化招呼语→限速投递；README沉淀Boss四层反爬（TLS/Canvas/WebGL指纹、鼠标键盘行为分析、zp_stoken网关验证、账号/IP风控引擎）的对应绕过方式，及风控错误码36(异常)/32(临时封禁)/37(环境异常)/1006(限速)的人工恢复手册。MIT。
- 引入理由：把『Camoufox+Boss』组合讲得最清楚的小而精样本：反爬分层对照表、风控错误码恢复流程、频率红线（搜索间隔3-5s、发送间隔5s、单次≤5条、每日≤20条）可直接搬进我们投递风控模块作缺省参数；附CLAUDE.md/AGENTS.md/SKILL.md三份agent配置，验证了该方案能在Claude Code中以『对agent说话』方式驱动，与目标运行形态一致。
- 融合方式：提取模块
- ⚠️ 限制：star仅24、体量小、2026-05后更新有限，代码成熟度不如头部项目；首次需camoufox fetch下载浏览器；指纹对抗是军备竞赛，Boss升级zp_stoken校验时需跟进上游Camoufox。

#### 🟢 daijro/camoufox
- 地址：https://github.com/daijro/camoufox（⭐12020）｜C++
- 核心能力：反检测Firefox浏览器：C++层伪造Canvas/WebGL/字体/分辨率/时区/地理一致性指纹，提供Python（Playwright兼容API）与JS SDK，内置humanize拟人鼠标行为、代理、指纹轮换；pip install camoufox[geoip] && camoufox fetch即用。MPL-2.0，2026-09仍活跃发版。
- 引入理由：反爬对抗方向的『上游基础设施』：与其自研指纹浏览器补丁，不如把指纹问题整体外包给这个1.2万star的活跃项目；Python端API与Playwright几乎一致，现有浏览器自动化代码迁移成本低，可作我们所有国内平台（Boss/猎聘/智联/51job）采集层的统一隐身底座，一处升级全平台受益。
- 融合方式：直接集成
- ⚠️ 限制：MPL-2.0为文件级copyleft（作为外部依赖调用不影响我们代码协议）；首次下载约百MB级浏览器包，需本地缓存；Firefox内核对Boss个别仅兼容Chrome的页面路径需实测；再强指纹也不能替代频率控制与账号健康度管理。

#### 🔵 Snseam/boss-zhipin-mcp
- 地址：https://github.com/Snseam/boss-zhipin-mcp（⭐136）｜Python
- 核心能力：Boss直聘MCP Server（FastMCP/stdio）+CDP附着已登录Chrome：多关键词轮询搜索+跨关键词去重、结果持久化本地JSON库（中断可恢复）、Canvas渲染简历截图绕过防爬、转发按钮+二维码解码提取zpurl.cn永久链接、YAML条件筛选+多维评分、一键生成Markdown报告。
- 引入理由：生态里最完整的『Boss x Claude Code』MCP接线范例：MCP工具划分、CDP复用真实登录态（免去cookie搬运与失效问题）、YAML驱动的评分配置结构、可恢复任务库、Markdown报告生成——这套架构模式可整体平移到求职者端；Canvas截图绕过对我们抓取JD/公司详情有直接参考价值。
- 融合方式：参考思路
- ⚠️ 限制：它是招聘者(HR)端工具，业务语义（搜人才、向候选人打招呼）与求职方向相反，只借架构不搬业务逻辑；README徽章声称MIT但仓库根目录无LICENSE文件；需本机Chrome+调试端口，无头服务器不可用。

#### 🟢 TreeWalk/boss-zhipin-skill
- 地址：https://github.com/TreeWalk/boss-zhipin-skill（⭐14）｜Python
- 核心能力：原生Claude Code Skill三件套：boss-setup一键配置CloakBrowser反检测环境并验证navigator.webdriver=false、boss-scrape多关键词触底滚动抓取（全局去重、渐进式限流恢复30s→90s）、boss-analyze用jieba+自定义技术词典从JD提取高频技能词并生成薪资/技能可视化HTML报告。MIT。
- 引入理由：与目标形态完全同构——它本身就是『装进Claude Code的Boss技能』，SKILL.md组织方式可直接参考；含两条硬核经验：Boss单关键词仅返回约90条结果，必须扩近义词+按链接全局去重才能起量；翻页靠触底滚动监测DOM卡片数变化而非改URL page参数。jieba动态技能词提取结果可直接喂给我们的五维匹配评分与ATS关键词层。
- 融合方式：直接集成
- ⚠️ 限制：star仅14的个人项目，需自行维护；依赖小众发行版CloakBrowser（供应链可信度建议评估，可替换为Camoufox）；抓取需有头浏览器并人工扫码登录一次；支持城市为内置的20个主流城市。

### 猎聘/智联/51job

#### 🟢 loks666/get_jobs
- 地址：https://github.com/loks666/get_jobs（⭐8465）｜Java (JDK21 + Selenium)
- 核心能力：全平台自动投简历脚本，对 boss、前程无忧(51job)、猎聘、智联招聘四平台各有独立实现类（src/main/java/boss/Boss.java、job51/Job51.java、liepin/Liepin.java、zhilian/ZhiLian.java）。驱动用户本机 Chrome，超长 Cookie 持久登录（大部分平台每周扫码一次），带企业黑名单自动去重、目标薪资筛选、企业微信推送通知、OpenAI 兼容接口生成打招呼语（实测每天约 $0.06 成本），附带 16 张表结构 + 673 条平台字典（城市/行业/经验/学历）的空数据库。
- 引入理由：这是唯一一个对猎聘+智联+51job 三个目标平台都有投递级实现且持续维护的高星项目，作者持续跟进各平台改版（README 更新日志标注 Boss 每日 150 次上限、51job 烂掉需投一页暂停 10 秒等实时状态）。其黑名单、投递记录库结构、平台字典可直接服务系统的投递风控与数据契约层。
- 融合方式：直接集成
- ⚠️ 限制：PolyForm Noncommercial 1.0.0 协议（禁商用）；各平台需扫码/Cookie 登录，猎聘仅微信扫码且需先在猎聘 App 最新版设置打招呼语；README 明确 51job「已经烂掉了不建议使用」（投递有上限、限搜索量、没什么活人 HR），Boss 最可靠，智联随改版波动；不能部署在服务器 IP（触发风控）；需自备 OpenAI 兼容 API Key。

#### 🟢 mergedao/mcp-jobs
- 地址：https://github.com/mergedao/mcp-jobs（⭐130）｜TypeScript (Node >= 16)
- 核心能力：基于 MCP 架构的多平台职位聚合 Server，支持猎聘、Boss、智联、51job 四平台职位搜索与详情获取，输出标准化格式；npx -y mcp-jobs 零配置启动，无需 API Key、无需登录，内置 crawler 配置（CRAWLER_* 环境变量控制 headless 浏览器行为）。MIT 协议。
- 引入理由：与 Claude Code 原生 MCP 配置直接对接，是把三个平台岗位采集接入系统的最短路径——写入 .mcp.json 即可在 /search 命令中直接调用；只读采集不投递，法律与风控风险最低，可作为多源聚合的标准化数据入口。
- 融合方式：直接集成
- ⚠️ 限制：只提供搜索/详情，不含投递能力；「零配置无登录」意味着走公开页面，智联/51job 反爬升级时可能随时失效，上线前必须实测；无认证登录态时能看到的岗位范围有限（如部分仅登录可见的职位）。

#### 🟡 xixiluo95/zhaopin
- 地址：https://github.com/xixiluo95/zhaopin（⭐119）｜JavaScript (Chrome Extension MV3 + Node.js 18 + SQLite)
- 核心能力：「招聘工作台 JobHunter」v2.2：Boss + 51Job + 智联三平台岗位列表与详情自动采集；V2 缓冲策略（大页列表+小批详情交替）规避反爬；encryptJobId/标题+公司+薪资三级去重；反爬触发后自动冷却→策略轮转→断点续爬；本地 CLI 入口 http://127.0.0.1:7893/enqueue；简历管理 6 模板 + DOCX 解析 + PDF 导出；AI 助手支持 8+ 模型，resume_ops 结构化编辑协议、岗位匹配评分、编辑版本回滚。
- 引入理由：51job 与智联目前最完整、仍在维护的采集实现（2026-06 发 v2.2）；其「缓冲策略+冷却续爬+三级去重」是经过实测的反爬工程方案，可直接移植到 Playwright 采集器；resume_ops 协议是现成的简历结构化编辑契约，适合系统双 Agent 简历链路复用；全数据本地 SQLite 存储，符合本地化铁律。
- 融合方式：提取模块
- ⚠️ 限制：仓库页面未标注开源 License（合码前需向作者确认）；自动投递仅 Boss 侧实现，51job/智联只有采集；架构是 Chrome 扩展 + 本机 Node 服务，移植到 Playwright 需要改造；扩展端需要各平台登录态。

#### 🟡 jolie-z/Auto-JobHunter
- 地址：https://github.com/jolie-z/Auto-JobHunter（⭐80）｜Python (FastAPI + LangGraph + DrissionPage + SQLite)
- 核心能力：工业级全自动 AI 求职系统，工作流：自动抓取 → SQLite 入库 → LLM 深度评估 → RPA 静默投递；LangGraph 多 Agent 评估管线 + DrissionPage 反检测浏览器自动化，覆盖 BOSS/Liepin/51job 三平台，Cookie 登录。
- 引入理由：与目标系统数据链路（爬取→结构化→评分→投递→追踪）完全同构；LangGraph 评估 Agent 的打分-否决式筛选、DrissionPage 静默投递的行为伪装、SQLite 数据契约都可直接借鉴，省去从零设计评分与投递调度的工作量；Python 技术栈比 get_jobs 的 Java 更贴合 Claude Code 环境。
- 融合方式：提取模块
- ⚠️ 限制：License 为 Custom（页面标注 License-Custom，合码前必须确认条款）；README 以 macOS 场景为主（Mac 提及远多于 Windows）；同样依赖各平台 Cookie 与本机浏览器；LLM 评估需自备 API Key；项目更新频率低于 get_jobs（最近 push 2026-04）。

#### 🔵 jiyangnan/AgentMesh-JobAgent
- 地址：https://github.com/jiyangnan/AgentMesh-JobAgent（⭐40）｜Python (CLI)
- 核心能力：Agent 原生求职 CLI，四条相互隔离的平台工作流：Boss直聘/猎聘/智联招聘/前程无忧51Job，单平台页面改版不影响其他平台。开源 CLI 通过用户本机浏览器会话连接平台，云端做 36 维候选人画像→逐岗 selected/review/rejected 签名决策（带理由与风险）→生成有依据的个性化打招呼语→CLI 校验签名后执行；投递前预览 + 用户确认 + audit 审计链；Codex/Claude 的 native Computer Use 任务协议（work next/begin/submit，不用 CDP、不碰隐藏 API）。
- 引入理由：三个目标平台全覆盖且是本次调研中更新最活跃的项目（调研当天仍在 push）；其「每条岗位决策带签名与理由、投递前预览+用户确认+审计、平台工作流故障隔离、任务协议不拦截浏览器外操作」的设计模式，正是系统投递安全层（原则二反爬优先）和可解释性需要的参考实现。
- 融合方式：参考思路
- ⚠️ 限制：核心决策能力在收费云端（免费注册仅送少量积分，Standard Pass ¥29.99/30 天 1000 积分），与「全部本地执行、禁止第三方云端」铁律直接冲突，只能借鉴设计不能引入依赖；需注册 AgentMesh360 账号与 API Key；积分制限制单轮投递量（每平台 100 岗/10 积分）。

#### 🔵 CODE-OF-ZHANG/JobGuide
- 地址：https://github.com/CODE-OF-ZHANG/JobGuide（⭐61）｜Kotlin (Android)
- 核心能力：基于 Android AccessibilityService（无障碍服务）的多招聘平台批量打招呼工具，支持 BOSS直聘/51job/智联/猎聘四平台。MIT 协议。
- 引入理由：目前唯一验证了「51job/智联/猎聘移动端批量投递可行」的开源实现——移动端 App 反爬明显弱于 PC Web 端，它按平台解析控件节点的思路证明了一条 PC 方案全灭时的替代路径；可作为系统的「移动端半自动降级方案」推荐给有安卓手机的用户。
- 融合方式：参考思路
- ⚠️ 限制：Android APK 无法与本地 Claude Code 系统做代码级集成；需要用户安卓手机 + 开启无障碍权限；投递行为发生在用户手机上，系统只能做任务下发与结果回收的编排建议。

### 校招垂直渠道（实习僧/牛客/应届生/国聘/央企/高校）

#### 🟢 Jasmine-Liu-min/job-radar
- 地址：https://github.com/Jasmine-Liu-min/job-radar（⭐19）｜Python
- 核心能力：面向2027届的多源校招信息台：信源覆盖国聘、央企公告、国家大学生就业平台、人社部公共招聘、大厂官网（字节/腾讯/京东/网易/蔚来）、10+所高校就业网（复旦/上交/浙大/中南/西电等）、牛客、实习僧、飞书招聘；带 fast/slow 双车道同步（Playwright 只用于牛客/实习僧/SPA高校等慢源）、新增对比、信源健康报告、牛客线索人工审核台、简历画像评分与 rescore、单页 HTML 工作台和 GitHub Actions 定时推送。
- 引入理由：这是本方向覆盖面最完整、工程最成熟的项目，几乎逐条命中'校招垂直渠道'需求：正式信源用 config/sources.csv 声明（加源只改CSV）、单源失败不阻断全链路、未装Playwright时慢源优雅降级、牛客/公众号/就业群走审核台不污染主库。其信源清单、nowcoder_discover.py 线索发现、import_feed.py 半结构化导入、信源健康报告可直接搬进本系统，省掉自建渠道层的大部分工作量。
- 融合方式：直接集成
- ⚠️ 限制：无LICENSE文件（代码默认保留所有权利，融合时以提取配置/思路为主并注明来源，或联系作者）；作者个人画像偏产品/数据/长沙方向，需替换 config/role_keywords.json 与评分规则；core链路主要用标准库，仅慢源需要 Playwright+Chromium。核验方式：2026-09-20 通过 GitHub API 元数据 + raw README 实际核验（本环境 WebFetch 被策略禁止访问 github.com）。

#### 🟡 jiabaobei/xiaozhao-radar
- 地址：https://github.com/jiabaobei/xiaozhao-radar（⭐15）｜JavaScript/Python
- 核心能力：AI驱动的校招信息聚合平台：维护856个校招节点（126家企业官网招聘页+16个官方公众号校招推文+714家公开校招节点，覆盖13行业，含国家电网/中石化/中石油/航天科工等50家央企与腾讯/阿里/字节等50家大厂）；仓库内置 jobs.json 数据集（约1598条27届校招信息，每周五从腾讯文档「27届实习提前批信息汇总」同步），支持行业/地点双维筛选、CSV导出、四级备用抓取工具策略。
- 引入理由：两层价值：其一，jobs.json 是现成的27届校招种子数据集（含行业分类：综合552/互联网科技367/装备重工322/银行金融126等），可直接导入本系统做冷启动与匹配算法验证；其二，856个校招节点URL清单本身就是'央企国企校招官网聚合'的数据源地图，免去逐个搜集企业校招官网的成本。
- 融合方式：提取模块
- ⚠️ 限制：Apache-2.0；前端是静态页，抓取层依赖 Firecrawl/AnySearch/BrowserAct/OpenCLI 等第三方工具（部分为云服务），与'全部本地执行'原则冲突，建议只取数据集与节点清单，抓取层用本系统自己的适配器；数据质量依赖人工周更，入库时需复核链接有效性。

#### 🟡 onism1767-creator/wenke-radar
- 地址：https://github.com/onism1767-creator/wenke-radar（⭐13）｜Python
- 核心能力：接入30+家公司共45个官方招聘接口（JSON API直连，非浏览器爬虫），自动抓取产品/运营/电商/市场/商分/人力等12类非技术岗的社招与2026/2027届秋招新岗位，每天早7:30经Server酱推送微信；GitHub Actions+Secrets 零服务器运行，MIT协议。
- 引入理由：本方向唯一做到'45个源全部走官方接口、零浏览器、零登录'的项目，是反爬优先原则的最佳实践样本：其45个接口的URL与参数配置是最有价值的可移植资产，可整体并入本系统的校招源池（尤其补齐快消/电商/文娱等非技术岗渠道，正好是 job-radar 自认的缺口）。README 还内置了一套'把配置步骤复制给任意AI逐步指导'的用户引导文案，可直接借鉴到本系统首次使用引导。
- 融合方式：提取模块
- ⚠️ 限制：MIT；岗位方向按文科非技术岗调优，接入本系统需改关键词/城市过滤；微信推送需免费注册 Server酱(sct.ftqq.com) 获取 SendKey；源接口为非公开承诺的公司内部API，存在随官网改版失效的风险，需配信源健康检查。

#### 🟡 ruyi1/campus-radar
- 地址：https://github.com/ruyi1/campus-radar（⭐7）｜Python
- 核心能力：每天自动抓取京东/快手/小红书/拼多多/淘天5家大厂校招官网+OfferStar聚合平台共6个数据源，SQLite存历史、diff识别新增岗位，按方向+城市过滤后生成Markdown日报，支持Server酱/企业微信机器人推送与投递进度追踪（可导出Excel）；GitHub Actions定时+数据库随仓库持久化。
- 引入理由：结构最清晰的大厂校招官网适配器参考实现：scrapers/base.py 统一数据结构+各公司独立scraper的模式，与本系统 adapters/ 目录设计完全同构，可直接对齐接口后逐文件移植；其'SQLite历史对比→只报新增→日报→投递追踪'闭环就是 /pipeline 命令的现成骨架。专为27届设计，与目标用户一致。
- 融合方式：提取模块
- ⚠️ 限制：无LICENSE文件（提取代码时需注明来源并注意法律风险）；源只有6个且偏产品/运营方向，渠道广度靠 job-radar/wenke-radar 补齐；GitHub Actions定时任务有延迟，临近截止的岗位不能只依赖推送。

#### 🟡 23aaaa/jobfill
- 地址：https://github.com/23aaaa/jobfill（⭐34）｜JavaScript (Chrome扩展)
- 核心能力：网申秋招一键填表Chrome扩展：简历数据集中管理在本地 resume-data.js，在牛客/智联/BOSS直聘/企业校招官网等任意网申页点字段按钮即自动填入，适配React/Vue/Angular渲染的表单与contenteditable，带字段搜索、跳转下一输入框、隐私模式，完全离线不上传。
- 引入理由：校招网申（企业官网ATS系统）是自动化投递中最难绕过的环节，本项目是目前star最多、且明确覆盖牛客网申场景的开源方案。融合价值在于数据契约层：本系统的 /profile 统一画像JSON可直接生成其 resume-data.js，形成'本地画像→浏览器半自动填表'的合规投递通路，作为Playwright自动投递被风控拦截时的优雅降级路径（人工确认+机器填表）。
- 融合方式：提取模块
- ⚠️ 限制：无LICENSE文件；它不是全自动投递（需人工点击字段按钮，属有意设计的人工确认门），不能直接挂进Claude Code流程，只能作为随系统分发的配套浏览器工具；部分网申系统的特殊控件（日期选择器/文件上传）仍需手动。

#### 🟢 JiemsLBJ/offer-harvester
- 地址：https://github.com/JiemsLBJ/offer-harvester（⭐3）｜Python/Bun(TypeScript)
- 核心能力：面向中国招聘市场的本地优先AI求职流水线，基于 MadsLorentzen/ai-job-search 框架扩展：把实习僧/腾讯招聘/Hotjob等站点封装为统一 search/detail CLI，Playwright复用本机Chrome登录态做表单适配，含去重、基于候选人事实的匹配排序、邮件草稿、人工确认门、本地进度看板；由Claude Code/Codex等读AGENTS.md的编码代理驱动。
- 引入理由：与本系统底座同源（都基于 ai-job-search），是'ai-job-search中国化'的现成答案——其站点搜索CLI、浏览器表单适配器、人工确认门、/setup个性化机制可以直接嫁接到本系统的career-ops+ai-job-search融合体上，避免重复造国内平台适配层。其'搜索整理自动化、事实核对与最终提交留给求职者'的边界设计也符合投递风控要求。
- 融合方式：直接集成
- ⚠️ 限制：MIT；项目较新star少（2026-09仍在活跃提交，有CI），成熟度依赖上游 ai-job-search；需要本机Chrome登录态（.profile隔离），/setup个性化后不得推公开仓库（含个人简历数据）；投递核心依赖Bun运行时（搜索CLI）与Python 3.10+双栈。

#### 🟡 jiacan233/shixiseng-auto-submit
- 地址：https://github.com/jiacan233/shixiseng-auto-submit（⭐3）｜Python
- 核心能力：实习僧网页版的AI岗位筛选与自动投递工具：按公司主页抓取岗位并翻页→先做时间/地点硬过滤→调OpenAI兼容接口对照候选人画像输出结构化投递建议（置信度/匹配项/风险点）→预演模式(dry-run)或保守自动投递，history.csv去重，独立.chrome-profile会话，遇滑块/验证码立即停止整个任务。
- 引入理由：实习僧是目标渠道中反爬较重（字体加密）且无成熟爬虫可用的平台，本项目提供了已验证的兜底路径：不硬刚接口而是复用本机登录态的浏览器自动化，并把'AI筛选+预演+验证码即停+仅投已有附件简历+歧义即停'整套保守投递风控实现出来了——这套风控模式可直接抽象为本系统 /apply 的通用安全层，不限于实习僧。
- 融合方式：提取模块
- ⚠️ 限制：MIT；需OpenAI兼容API Key与本地Chrome登录实习僧账号；非官方工具，存在账号风控/违反ToS风险，README明确要求先预演模式复核；仅覆盖'按公司批量投递'场景，不支持全站关键词搜索采集（采集侧建议用 s0meb0dy3/shixiseng-job-csv 思路或手动导入）。

#### 🟢 hrj-11055/guangdong-public-jobs-monitor
- 地址：https://github.com/hrj-11055/guangdong-public-jobs-monitor（⭐4）｜Python
- 核心能力：以Claude/Codex Skill形式交付的公开招聘信息源监控：预置55个来源入口（国家/省/21地级市/广州11区+教育/卫健/国资行业源），stdlib-only的monitor.py做每日变化检测，输出daily-report.md、notices.csv、source_health.csv、state.json去重状态，配Excel台账模板、SKILL.md执行规范、GitHub Actions每日双跑并在发现新公告时自动建Issue。
- 引入理由：它是'国企/事业单位/体制内'渠道的现成Skill，且工程范式值得整体照搬：--baseline基线机制避免首次运行误报、每源健康证据留痕、'自动发现只是线索层，报名条件须人工复核'的定位声明，正是本系统信息完整性与降级设计需要的模板；55源清单+SKILL.md结构可直接复制为本系统的 province-jobs-monitor 技能，替换sources.json即可扩展到其他省份。
- 融合方式：直接集成
- ⚠️ 限制：无LICENSE文件；来源清单为广东/广州本地化（跨省使用必须重建sources清单）；SKILL.md按Codex技能写法，接入Claude Code需微调触发方式；监控脚本只做网页发现层，岗位报名细节仍需打开官方公告人工核验。

### Claude Skill 生态

#### 🟢 anthropics/skills（官方 Agent Skills 仓库与编写规范）
- 地址：https://github.com/anthropics/skills（⭐177211）｜Markdown（SKILL.md）
- 核心能力：官方技能仓库：含 spec/agent-skills-spec.md（指向 agentskills.io/specification 的权威格式规范）、template/SKILL.md 最小模板，以及 19 个官方技能，其中 docx/pdf/xlsx/pptx 文档技能可直接生成真实 Office/PDF 文件，skill-creator 可辅助编写新技能。
- 引入理由：这是整个系统 SKILL.md 写法的对齐基准（frontmatter 仅 name/description，progressive disclosure，正文 <500 行）；docx/xlsx/pdf 技能直接解决「定制简历必须产出可投递文件」这一环，无需自研文件生成；skill-creator 可用来批量生成 /profile /search /apply 等六个入口技能的骨架。
- 融合方式：直接集成
- ⚠️ 限制：仓库根目录无 LICENSE 文件（仅 THIRD_PARTY_NOTICES.md），引用技能内容时保留出处即可但商用条款需自查；官方 spec 已外迁 agentskills.io，以该站为最终格式依据。已实际 git clone 并核对 spec/template/docx 技能内容。

#### 🟡 Paramchoudhary/ResumeSkills
- 地址：https://github.com/Paramchoudhary/ResumeSkills（⭐2362）｜Markdown（纯 SKILL.md 集合）
- 核心能力：22 个求职技能组成的最完整技能集：job-description-analyzer（匹配分+缺口）、resume-tailor（强调 truthfulness 的定制）、resume-ats-optimizer、resume-bullet-writer/quantifier、resume-version-manager、interview-prep-generator、offer-comparison-analyzer、salary-negotiation-prep、application-form-filler、cold-email-writer 等，每个都是单文件 SKILL.md，统一「When to Use → Core Capabilities → 流程」结构。
- 引入理由：覆盖了本项目 /search /apply /interview /upskill 所需的几乎全部分析逻辑，可直接挑 6-8 个改写成中文校招版（加入校招关键词、应届生结构），省去从零设计评分与 ATS 规则；其 resume-tailor 明确写了「HIGHLIGHTING not fabricating」，与项目真实性铁律天然一致。
- 融合方式：提取模块
- ⚠️ 限制：英文导向、无脚本无代码（纯提示词技能），ATS 评分是启发式而非可复现算法；面向社招较多，需自行加校招过滤。已 clone 并核对多个 SKILL.md。MIT。

#### 🟡 yanliudesign/offer-toolkit-skill
- 地址：https://github.com/yanliudesign/offer-toolkit-skill（⭐466）｜Markdown（中文，HTML 模板渲染）
- 核心能力：中文原生求职技能包：顶层 router SKILL.md + 三个自包含子技能——job-description-skill（JD 解码，输出 HTML 版 Offer Strategy 报告：值不值得投/匹配度/技能缺口/大概率面试题/薪资合理性/六周准备计划）、resume-skill（11 套可打印简历模板，含 Classic-ATS、Tech Compact、Swiss，每套出 print 版+可编辑版 HTML）、bq-skill（STAR/CAR 故事库+按 JD 预测 20 道行为面问题），共享三条规则：绝不编造 / 一次只问一个问题 / 先结构后渲染。
- 引入理由：是目前中文生态里结构最规范的求职 skill bundle（router + 子技能分层正是本项目主入口 SKILL.md 该有的形态）；JD 解码报告与「六周计划」可直接变成 /search 与 /upskill 的输出格式；11 套模板解决中文简历排版；三条共享规则可直接抄进系统全局约束。声明兼容 Claude Code/Codex/OpenCode 等多 Agent 运行时。
- 融合方式：提取模块
- ⚠️ 限制：git clone 在本环境两次超时（模板资源较大），已改经网页完整核对 README 与目录结构（v1.0.0，MIT，2026-09 仍在更新）；非校招专属，社招语气需微调。

#### 🟡 spontaneousai/job-hunt-copilot
- 地址：https://github.com/spontaneousai/job-hunt-copilot（⭐252）｜Markdown（中文）
- 核心能力：「素材库」驱动的中文求职技能：把用户档案（self_profile.md）、基础简历（resume_base.md）、项目历程库（projects/*.md）作为唯一事实来源，每次投岗从素材库取材重写 bullet、按产品/运营/研发等角度重构项目叙事、生成中英双语项目讲稿 docx、交互式模拟面试（友好复盘/高压追问两种模式）。以 .skill 包分发，内含标准 SKILL.md + resources/ 模板。
- 引入理由：它是原则三（事实不可捏造）在技能层最干净的实现范式：所有输出只能引用素材库条目，天然可审计，可直接改造成本项目统一的用户画像 Schema 与 data/ 目录契约；「项目讲稿 + 可能追问」比通用面试题库更贴合应届生项目深挖场景。
- 融合方式：提取模块
- ⚠️ 限制：仓库无 LICENSE 文件（默认版权保留，借鉴架构与模板措辞、注明出处较稳妥）；需要先解包 .skill（zip）取内容；不含岗位采集能力。已 clone 并解包核对 SKILL.md 与 resources。

#### 🔵 squerne/open-career-skills
- 地址：https://github.com/squerne/open-career-skills（⭐24）｜Markdown（Claude Code workspace，含 prompts/hooks）
- 核心能力：8 个以「拒绝编造」为核心设计的求职技能 + /apply 流水线：ruthless-cv-optimizer（先诊断提问再改写，缺口如实报告）、evidence-miner（无 commit/PR 等可引用证据不产出成就）、job-search（只报告真实抓到的岗位，抓不到就给搜索 URL，绝不编造 listings）、ats-detective（实测 ATS 厂商，「探测不到」不得报成「不存在」）、mock-interviewer，以及独立 reviewer subagent 逐条核对草稿与档案、还原被改动的数字；profile/ story-bank/ tracker/ 文件化记忆。
- 引入理由：架构上与本提示词三条铁律一一对应：双 Agent 审核=它的 reviewer subagent，降级不装成功=job-search/ats-detective 的诚实规则，素材库=evidence-miner+story-bank。star 虽少但设计密度全场最高，/apply（评估→起草→审核→记录）可直接作为本项目 /apply 的状态机蓝本。
- 融合方式：参考思路
- ⚠️ 限制：24 star，海外平台导向（Greenhouse/Lever/LinkedIn），国内平台需整体替换数据源；已 clone 并核对 README 与 prompts/。MIT。

#### 🔵 sliday/resume-job-matcher
- 地址：https://github.com/sliday/resume-job-matcher（⭐271）｜TypeScript（附 legacy Python）
- 核心能力：简历-岗位筛选 CLI，核心是三级漏斗：pre-filter（embedding 余弦粗排，每份简历只 embed 一次）→ Gate（JD 硬性条件转成带严重级别的 pass/fail 问题，命中否决项即淘汰并引用简历原文，沉默不视为不通过）→ Score（每条标准按锚定 0-4 打分，每个分都必须引用简历原文作证据）。口号：法律上干不了这活的人不该得 62%。带 evals/ 测试目录。
- 引入理由：把「评分可复现、可解释」做成了工程：硬性门槛前置（自动排除要求 1 年经验/非校招岗，正好是 3.1 强制过滤规则的实现思路）、锚定量表（五维评分每个维度写打分锚点）、证据引用（每分给出岗位原文出处）。整体反向使用即是求职者侧的岗位筛选器。
- 融合方式：参考思路
- ⚠️ 限制：方向相反（招聘方筛人），需反向移植其 gate/score 提示词；依赖 OpenRouter/Anthropic/OpenAI API key，本地化需改接本地模型或改为纯提示词版；仓库未附 LICENSE 文件。已 clone 并核对 readme/evals。

#### 🔵 obra/superpowers
- 地址：https://github.com/obra/superpowers（⭐288929）｜Markdown（SKILL.md）+ hooks/scripts
- 核心能力：最流行的技能工程范本（15+ 技能）：writing-skills 是「给技能写 TDD」的元技能——先写失败基线（无技能时 agent 如何偷懒/找借口），再写技能，再用子代理压力测试验证合规，并给出反合理化话术写法；verification-before-completion 强制先验证再宣布完成；另有 brainstorming、systematic-debugging、subagent-driven-development、dispatching-parallel-agents。
- 引入理由：直接服务原则五「交付即验证」：verification-before-completion 可移植为每个模块完成前的自检门禁；writing-skills 的基线-验证循环是保证我们的 SKILL.md 真的会触发、真的约束住「不编造」的方法论（否则技能只是看起来有用）。
- 融合方式：参考思路
- ⚠️ 限制：是软件开发方法论而非求职工具，只取 2-3 个元技能移植；hooks 部分与 Claude Code 版本耦合较强。已 clone 并核对 skills/ 与 writing-skills 全文。MIT，2026-09 仍高频更新。

#### 🟡 cosen1024/resume-builder-skill
- 地址：https://github.com/cosen1024/resume-builder-skill（⭐45）｜Markdown + Python（WeasyPrint）
- 核心能力：以 Markdown 为内容源、多套模板（compact/classic/modern 等）多配色、可选证件照的中文简历技能，WeasyPrint 离线渲染成可搜索的 A4 PDF；内置 STAR/量化润色与 JD 定向匹配（输出命中/缺失关键词清单），中文字体内嵌、无 CDN 无远程服务，并自带 evals 测试脚本。
- 引入理由：完全满足「全部本地执行、无云端上传」：整条 Markdown→HTML→PDF 管线可离线复现，比 LaTeX 对用户更友好（不需要装 TeX 发行版）；缺失关键词清单可直接喂给 /upskill 生成技能缺口学习计划；其内容源与样式分离的设计便于统一数据契约。
- 融合方式：提取模块
- ⚠️ 限制：PolyForm Noncommercial 1.0.0（个人求职使用无碍，再分发进仓库需保留许可声明、不可商用）；需本地安装 Python+WeasyPrint 并配置中文字体；star 少，已通过网页核对 README 全文（git clone 在本网络反复超时）。

### MCP Server 生态

#### 🟢 gzchenhao/openhire
- 地址：https://github.com/gzchenhao/openhire（⭐6）｜Python (>=3.11)
- 核心能力：Agent 原生求职 MCP server（PyPI 包 openhire，CLI 命令 ohp）：直接对接 139 家雇主自己的 ATS 公开 API（Greenhouse/Lever/Ashby + 中国的北森 Beisen/Moka），覆盖美欧+中国（宇树、小鹏、优必选、梅卡曼德、速腾聚创等20+家机器人/自动驾驶公司）。提供 search_jobs / watch_intent / check_watches / authorize_application / get_company_info 五个工具，每个职位带 ghost_score（幽灵岗位评分）、verified_at 真实发布时间、官方投递直达链接。`uvx openhire@latest serve` 即可接入，匹配全部本地执行、简历永不上传。
- 引入理由：这就是任务里提到的 OpenHire 的真实本体。它是极少数官方支持中国 ATS（北森/Moka）的 MCP server——校招最集中的恰恰是这些企业官网渠道；ghost_score 和『以雇主 ATS 真实发布时间为准』的设计直接解决招聘平台上职位过期/长期挂着的痛点，对应提示词里『自动排除已过期岗位』的硬需求；协议里结构性禁止简历字段，完全符合『数据全本地』铁律。
- 融合方式：直接集成
- ⚠️ 限制：仅 6 star、项目很新（由 Claude Code 构建的个人项目），成熟度未经大规模验证。岗位池偏向 AI/机器人/自动驾驶 139 家雇主，不是全行业校招覆盖。飞书招聘(Facebook Hire)明确不支持（签名+验证码反爬，项目拒绝绕过）。首次启动下载约 25MB 公共快照，实时性取决于其每周刷新节奏。注意 GitHub 上有大量同名无关项目（SilkRoad 商业ATS、openhire.cc 商业网站），勿混淆。

#### 🟡 stickerdaniel/linkedin-mcp-server
- 地址：https://github.com/stickerdaniel/linkedin-mcp-server（⭐3550）｜Python (FastMCP + Patchright)
- 核心能力：LinkedIn 领域 star 最多的 MCP server（Apache 2.0）。复用你本机已登录的浏览器会话驱动 Patchright Chromium，提供 19 个工具：search_jobs/get_job_details、人物/公司搜索与画像抓取、站内信收发、feed 读取等。全部浏览器动作本地执行，支持 uvx 一键运行、Docker、MCP Bundle、stdio 与 streamable-http 两种传输。
- 引入理由：求职类 MCP 里工程质量最高的参考实现：会话管理（--import-from-browser 复用本机登录、会话过期检测）、多客户端共享浏览器的串行化锁、超时分层（页面/工具/登录三级）、以及明确的责任边界与风控提示文案，都是给 Boss直聘/猎聘浏览器自动化适配器写代码时可以直接照抄的工程范式。search_jobs/get_job_details 也可作为海外岗位/外企渠道的直接数据源。
- 融合方式：提取模块
- ⚠️ 限制：需要真实 LinkedIn 账号登录，README 自认自动化访问违反 LinkedIn 用户协议、有封号风险（对 2027 届校招主战场价值有限，仅外企/海外岗需要）。国内网络环境需代理。维护节奏快但依赖 LinkedIn 页面结构，页面改版即坏。

#### 🟡 borgius/jobspy-mcp-server
- 地址：https://github.com/borgius/jobspy-mcp-server（⭐115）｜JavaScript (Node) + Python (JobSpy)
- 核心能力：把 JobSpy（Python 多平台职位抓取库）封装成 MCP server：一次 search_jobs 调用可跨 Indeed、LinkedIn、Glassdoor、ZipRecruiter、Google、Bayt、Naukri 七个平台搜索，支持 hours_new、results_wanted、地区等参数，输出 JSON/CSV，支持 stdio 与 SSE 传输、Docker 部署。
- 引入理由：真正的价值在其底层依赖 JobSpy 这个成熟抓取库（ Indeed/LinkedIn/Glassdoor 等的结构化解析已有人维护），比自写爬虫可靠得多。海外岗位/外企校招（尤其外企中国区挂在 Indeed/Glassdoor 的职位）可用它做第二数据源，与 OpenHire 的雇主直连形成互补。
- 融合方式：提取模块
- ⚠️ 限制：该 repo 工程较糙：通过固定相对路径 ../jobSpy/run.sh 调用外部 Python 脚本，与作者目录结构强耦合，直接跑通成本高。更稳妥的做法是只提取思路、直接 pip 安装 jobspy 库自己包一层 MCP。爬 Indeed/LinkedIn 有反爬与限流风险，仅覆盖海外岗位。

#### 🟢 amikai/openings-mcp
- 地址：https://github.com/amikai/openings-mcp（⭐92）｜Go
- 核心能力：单二进制 MCP server，统一搜索职位板与公司官网：覆盖 104人力银行、Cake（台湾）、Jobindex(丹麦)、マイナビ(日本)、LinkedIn/Indeed 公开搜索，以及 41000+ 家托管在 Workday/Ashby/Greenhouse/Lever/Teamtailor/SuccessFactors/MokaHR 等约 20 种 ATS 上的公司 careers 页，外加 Amazon/Apple/Google/Meta/TSMC 专属适配。无需登录任何账号，`claude mcp add --scope user openings-mcp -- openings-mcp` 即接入。
- 引入理由：目前覆盖面最广的『公司官网校招页』聚合器——校招的主战场就是企业官网，而它把 Workday/Greenhouse/Lever 等 ATS 的公开端点抽象成了统一接口。它的 ATS provider 抽象（每种 ATS 一个适配器 + 公司名册）是给 Boss直聘/猎聘/北森/Moka 写统一适配层时最值得照抄的架构；TSMC/Amazon 等专属适配也证明『单公司硬编码适配』路线可行，可复制到国央企官网。MIT 协议。
- 融合方式：直接集成
- ⚠️ 限制：免责声明明确是 unofficial，调用各站公开未文档化端点，随时可能改版失效；无内置限流，高频调用会触发目标站 IP 封锁，只能人工频率使用。中国区仅覆盖 MokaHR ATS，无北森。Go 编写，二进制拿来即用但二次开发需要 Go 技能。注意 token 消耗快（README 自己提醒）。

#### 🔵 workopia/workopia-mcp
- 地址：https://github.com/workopia/workopia-mcp（⭐316）｜JavaScript
- 核心能力：托管型（远程）求职 MCP server：streamable-http 端点 https://workopia.io/api/mcp-jobs + OAuth 登录，提供 90+ 国家职位搜索(job_tool)、职位详情、简历定制(tailor_resume_tool)、求职信生成(cover_letter_tool)、申请看板(dashboard_tool) 五个工具。AI 推理由客户端本地模型完成，服务端不托管 LLM、不收费。
- 引入理由：展示了完整的『求职 MCP 产品化工具面设计』：把搜索/详情/简历定制/求职信/申请追踪切成五个粒度合适的 tool，正是 Campus-Job-Agent 六命令(/search /apply /pipeline)在设计 MCP tool 边界时的直接参考；其 Claude Code plugin（/plugin marketplace add）分发方式也值得借鉴。简历定制逻辑放在客户端模型执行、服务端只管岗位数据的分工，与本地化原则兼容。
- 融合方式：参考思路
- ⚠️ 限制：与『全部本地执行、数据不传第三方云』铁律直接冲突：必须 OAuth 登录 Workopia 云端账户，搜索与简历数据都过它的服务器。托管端点可能限流、改版或收费。因此只借鉴工具设计与 plugin 打包方式，不建议直接接入。

#### 🟢 modelcontextprotocol/python-sdk
- 地址：https://github.com/modelcontextprotocol/python-sdk（⭐24.3k）｜Python (>=3.10)
- 核心能力：MCP 官方 Python SDK（MIT），支持 stdio / Streamable HTTP / SSE 全部标准传输。v2 已发布（对应 2026-07-28 协议规范）：15 行即可写一个完整 MCP server——`from mcp.server import MCPServer`，函数加 `@mcp.tool()` 装饰器（类型注解即 JSON Schema，无需手写协议代码），`uv run mcp dev server.py` 可在 MCP Inspector 里调试。
- 引入理由：这是写自研 campus-job-mcp server 的标准脚手架：当 mcp-jobs / OpenHire / openings-mcp 覆盖不到的渠道（实习僧、牛客、应届生求职网、国聘、央企校招官网、校园就业信息网）时，用这个 SDK 各写一个几十行的 stdio server，Claude Code 通过 .mcp.json 原生调用即可，不必放弃 MCP 生态去写散装脚本。官方维护、文档完整（py.sdk.modelcontextprotocol.io），长期可靠。
- 融合方式：直接集成
- ⚠️ 限制：只是脚手架，不含任何求职逻辑，平台适配全部要自己写（反爬仍需遵守『反爬优先于硬刚』原则）。注意 v2 刚改版（MCPServer 新 API），网上大量旧教程是 v1 的 `from mcp.server.fastmcp import FastMCP` 写法，混用会踩坑；依赖旧代码时按 README 指引先锁 `mcp<2`。Node/TS 侧对应官方 modelcontextprotocol/typescript-sdk（mcp-jobs 即基于 TS 生态）。

### 简历生成与ATS

#### 🟡 srbhr/Resume-Matcher
- 地址：https://github.com/srbhr/Resume-Matcher（⭐28473）｜Python + Next.js/TypeScript
- 核心能力：最成熟的开源"主简历→JD→定制简历"管线：上传 master resume (PDF/DOCX) 解析为结构化数据，粘贴目标 JD，由 LLM 生成针对该岗位的改写内容与求职信，可自定义模板排版后导出专业 PDF。明确支持本地与远程 LLM（Claude、DeepSeek、Kimi、GLM、Gemma 等百余种），可全本地运行。
- 引入理由：它是被验证过 2.8 万 star 规模的"结构化数据→定制简历"完整管线范本，与本项目 /apply 命令的数据流（profile.json → JD 分析 → 定制简历 → PDF）几乎一一对应。其 master resume（单一事实底稿）+ 按 JD 生成变体的设计，正是提示词里"同一事实底稿、多岗位分别生成"的最佳实践参考；PDF/DOCX 解析与模板导出部分可直接复用，省去自研解析器。
- 融合方式：提取模块
- ⚠️ 限制：完整应用是 Next.js+Python 的 Web 栈，作为依赖整包运行偏重，建议提取其解析（PDF/DOCX→JSON）与 JD 匹配逻辑而非跑整个 app；需要配置 LLM（可用本地或 Claude API）；社区活跃但功能迭代快，接口易变。

#### 🟢 reactive-resume/reactive-resume
- 地址：https://github.com/reactive-resume/reactive-resume（⭐43202）｜TypeScript (Node/NestJS + React)
- 核心能力：最大的开源简历编辑器：15 套模板、所见即所得编辑、A4/Letter 导出 PDF、DOCX 和 JSON 导出，支持从 JSON Resume 标准格式导入，可完全自托管，MIT 协议，无追踪。
- 引入理由：解决了自研系统最难啃的"结构化数据→像素级排版 PDF"问题：artboard/模板渲染引擎 + 结构化样式规则（Style Rules）是提取重点，可直接把 profile.json 渲染成 ATS 友好的中文 PDF。它也支持 JSON Resume 格式互导，可作为本系统的本地渲染服务（docker compose 全本地部署）。活跃度极高（2026-09 仍在发版 v5.3）。
- 融合方式：直接集成
- ⚠️ 限制：自托管栈较重（需 Postgres 等依赖，docker compose 一键但占资源）；中文 CJK 字体在 PDF 导出中的嵌入效果需实测；编辑器 UI 部分对本项目无用，只取渲染管线。

#### 🟢 coinluu/resume-jd-optimizer-cn
- 地址：https://github.com/coinluu/resume-jd-optimizer-cn（⭐181）｜纯 Skill（SKILL.md + 提示词 + 测试用例）
- 核心能力：JD 驱动的中文简历优化 Skill：解析目标 JD（硬性门槛/核心职责/加分项/淘汰风险）→ 简历事实底稿诊断 → 建立 JD 要求与真实经历的逐项证据映射（已覆盖/弱覆盖/未覆盖/不建议硬凑）→ 追问素材 → 生成 JD 定制版 + ATS 纯文本版 + HR 摘要 → 执行 HR、面试官、真实性三重审查。MIT 协议，带 10 个测试用例与 21 个失败模式清单。
- 引入理由：与本项目铁律"事实不可捏造"和双 Agent 审核机制高度同构：它明确规定无证据的关键词不算覆盖、用贡献动词证据门槛阻止"参与"变"主导"、用户要求造假时停止评分并给出替代方案。这套评分规则、证据映射方法和失败模式清单可直接并入 resume-builder 技能的审核环节，其 ATS 纯文本版输出是中文网申系统的刚需。
- 融合方式：直接集成
- ⚠️ 限制：2026 年新项目、star 少（181）、单作者；纯提示词工程，无代码解析/PDF 渲染能力，只解决"写什么、怎么审"，输出格式问题需配合其他项目；Boss直聘开场白部分与本项目 skills/resume-builder 可能重叠，注意去重。

#### 🟢 joeseesun/qiaomu-campus-resume
- 地址：https://github.com/joeseesun/qiaomu-campus-resume（⭐86）｜Python + Skill（含验证脚本）
- 核心能力：专做大学生/应届生简历的 Skill：一问一答深挖求职经历（个人贡献→行动→结果→证据→面试可解释性，A-C-R-E 审校），事实确认后由 resume-data.json 渲染为单页 A4 PDF——拉丁/CJK 分层字体、字体嵌入、文本可提取，六套主题（ATS 经典/瑞士现代/技术工程/校园清新等），带确定性验证脚本（assess_interview.py + validation.json），全本地处理、无需账号和 API key。
- 引入理由：这是目前唯一直接命中"中国校招应届生简历"的完整管线：中文排版细节（表格数字对齐、CJK 字阶、单栏 ATS 友好、禁止照片/侧栏）是踩过坑的实战经验，其 interview-ledger.json→resume-data.json→resume.pdf→validation.json 的确定性交付链路可直接嫁接进 /apply 和 /profile 命令。六套主题由同一份 JSON 生成且事实不变的架构，正是"结构化数据→定制简历"的教科书实现。
- 融合方式：直接集成
- ⚠️ 限制：star 少（86）、单作者、2026 年新项目；LICENSE 为自定义文本（GitHub 标记 NOASSERTION），合并代码前需人工审阅协议条款；以 Skill 形式交付，Python 部分仅验证脚本，渲染依赖其 HTML/PDF 生成流程需拆解。

#### 🟢 Jichengyuuuuu/resume-builder-skill
- 地址：https://github.com/Jichengyuuuuu/resume-builder-skill（⭐158）｜纯 Skill（单个 markdown 提示词文件）
- 核心能力：中文简历生成 Skill：任意素材（旧简历/JD/工作流水账/项目文档）一次性输入，信息收集后生成 HTML（可打印 PDF）+ DOCX 双格式简历，内置 11+ 岗位类型的技能维度逻辑（后端/AI产品/运营/设计等各按岗位动态调整），JD 关键词自动对齐做 ATS 优化，附高阶顾问求职建议。MIT 协议。
- 引入理由：本项目 /apply 输出格式清单里缺 DOCX，而国内网申（国企、央企、银行、部分大厂）普遍要求 Word 可编辑简历——它是轻量中文 DOCX 简历生成的现成提示词方案，MIT 无协议风险。其"按岗位类型动态决定技能维度写法"的矩阵设计（而非套固定模板）比通用简历提示词更贴合校招多方向投递场景。
- 融合方式：直接集成
- ⚠️ 限制：同样 star 少（158）、单作者；纯提示词、无校验脚本和确定性输出（相比 qiaomu 的 validation 体系弱）；AI 生成风格偏浓，需配合本项目真实性审核环节过滤。

#### 🟡 xitanggg/open-resume
- 地址：https://github.com/xitanggg/open-resume（⭐8908）｜TypeScript (React + react-pdf/pdf.js)
- 核心能力：开源简历构建器 + 简历解析器双功能：构建器全浏览器本地运行（数据不出浏览器）；解析器在客户端用 pdf.js 解析已有 PDF 简历为结构化数据（工作经历、教育、技能等字段），用于检查 ATS 可读性（对照 Greenhouse/Lever 等主流 ATS 的解析习惯）。
- 引入理由：解决了 /profile 命令的冷启动问题：用户手里已有的 PDF 简历需要无损转成 profile.json 结构化数据，其客户端解析逻辑（pdf.js 分栏识别、字段抽取、即解析即预览校对）是现成方案且天然满足"数据不出本地"的隐私铁律。它对 ATS 解析规则的归纳（单栏、标准章节名、无表格）也可提炼为本项目 ATS 检查清单。
- 融合方式：提取模块
- ⚠️ 限制：AGPL-3.0 强传染协议，提取其代码会使相关模块必须 AGPL 开源（对个人本地工具无实际影响，但需保留协议声明）；2024-10 后无提交，已约两年未维护；解析器按美式简历设计，中文简历（姓名/籍贯/政治面貌等字段）需要二次适配。

#### 🟢 jsonresume/jsonresume.org (含 resume-cli 与 @jsonresume/ats-validator)
- 地址：https://github.com/jsonresume/jsonresume.org（⭐4716 (原 resume-cli 仓库)）｜TypeScript (Turborepo monorepo)
- 核心能力：JSON Resume 标准生态的官方 monorepo：统一的简历 JSON schema 与校验器（@jsonresume/schema）、命令行工具（packages/cli 即 resume-cli，支持渲染主题导出）、官方主题、以及新增的 @jsonresume/ats-validator 包； dozens of community themes 可将同一份 JSON 渲染为多种 HTML/PDF。
- 引入理由：为本系统提供"统一数据契约"的最佳候选格式：提示词要求所有个人信息/简历用统一本地 JSON Schema（统一用户画像 Schema），直接采用 JSON Resume 标准可白得 schema 校验、多主题渲染和生态兼容（Reactive Resume 也支持导入该格式），避免自造格式导致后续每一步都要写适配。ats-validator 包则提供了 ATS 规范检查的标准化实现。
- 融合方式：直接集成
- ⚠️ 限制：schema 面向国际简历，缺少中国校招特有字段（政治面貌、籍贯、应届届别、四六级等），需做扩展 schema（标准允许 custom 字段）；npm 包随 monorepo 迁移（resume-cli 已并入 jsonresume.org 的 packages/cli），引用时认准 @jsonresume scope；主题对中文排版支持参差不齐，需选型或自写主题。

#### 🔵 geekcompany/ResumeSample
- 地址：https://github.com/geekcompany/ResumeSample（⭐28311）｜Markdown 模板集（无代码）
- 核心能力：中文程序员简历事实标准模板系列（2.8 万 star）：按 PHP/iOS/Android/前端/Java/C++/Node/架构师等岗位分列的 Markdown 简历模板，内含模块结构（个人信息/教育/技能/经历）与中文技术简历的写作规范（措辞、STAR 式描述）。
- 引入理由：它是国内 HR 和面试官最熟悉的中文技术简历结构与表达范式，可作为本项目简历生成提示词的"写作规范层"参考：章节顺序、中文表述习惯（避免直译腔）、技术栈列举方式等，让 AI 生成的中文简历读起来不像 AI 生成。因无 License，只借鉴结构与写作思路，不复制模板文本。
- 融合方式：参考思路
- ⚠️ 限制：无 LICENSE（默认保留所有权利），不可直接复制其模板文本进系统输出；内容多年未大更新（2024 年后基本静止），部分技术栈举例陈旧；只有模板没有生成管线。

### 面试准备

#### 🟡 Snailclimb/JavaGuide
- 地址：https://github.com/Snailclimb/JavaGuide（⭐158,713）｜Markdown 内容 / Java 后端方向
- 核心能力：中文后端面试第一题库：计算机基础、Java/数据库/分布式/高并发/系统设计/AI应用开发的系统化问答与知识梳理，持续更新中（2026-09 仍有提交，Apache-2.0 协议）。
- 引入理由：本系统 /interview 目前只按 JD 关键词生成问题框架（core/interview.py），缺少真实、权威的八股答案内容。JavaGuide 提供结构化 Markdown 题解，可离线建成本地题库索引，让 Claude 出题后能引用标准答案核对用户回答，并把「岗位JD技能词 ↔ 题库章节」做成映射，直接提升技术基础板块的深度与可信度。
- 融合方式：提取模块
- ⚠️ 限制：内容偏 Java/后端，前端/AI岗覆盖弱；Apache-2.0 已在仓库页面确认，融合时保留版权声明即可。

#### 🟡 febobo/web-interview
- 地址：https://github.com/febobo/web-interview（⭐11,865）｜Markdown (VuePress 站点)
- 核心能力：社群维护的前端面试题库，15 个系列（Vue/Vue3/ES6/JS/CSS/webpack/HTTP/NodeJS/React/TS/操作系统/算法/小程序/设计模式），每题以「面试官：」格式组织问答，配在线站点。
- 引入理由：补齐前端方向应届生的题库空白：其「面试官问-标准答」的结构几乎可直接映射为本系统的 question bank JSON schema（question/answer/topic 字段一一对应），ingest 成本极低；还能反向用于 /search 岗位评分——用题库 topic 词表增强 JD 技能词识别。
- 融合方式：提取模块
- ⚠️ 限制：2025-05 后更新放缓（内容本身仍通用）；仓库页面未标注开源协议，建议仅本地个人使用并保留出处，不做再分发。

#### 🟡 xiaolincoder/CS-Base
- 地址：https://github.com/xiaolincoder/CS-Base（⭐18,414）｜Markdown 内容
- 核心能力：小林coding《图解网络/操作系统/MySQL/Redis》与「面试突击版」，1000 张图+50 万字，公认质量最高的中文计算机基础八股讲解，配套 xiaolincoding.com 在线阅读。
- 引入理由：本系统技术基础板块对「网络/OS/数据库」只生成通用问题、无参考答案；CS-Base 是应届生面试这些主题时被追问概率最高的内容源，用它的目录结构可以给 /interview 生成带追问链的深度题目（如 TCP 握手→为什么三次→TIME_WAIT），显著提高模拟面试的真实压迫感。
- 融合方式：提取模块
- ⚠️ 限制：CC BY-NC-ND 4.0：禁商用、禁演绎——只能原样引用并注明出处，不可改写后分发（个人本地使用可行）；仓库 push 停在 2024-11，但基础内容不过时。

#### 🟢 jennifer88huang/interview-skills
- 地址：https://github.com/jennifer88huang/interview-skills（⭐347）｜JavaScript（Claude Code Skill）
- 核心能力：专为 Claude Code 设计的模拟面试教练技能：按 JD+简历生成个性化面试题、进行多轮交互式模拟面试（算法/系统设计/行为面/领域知识）、给出结构化评分反馈与改进建议，并支持跨场次进度追踪。
- 引入理由：与本项目技术栈完全同构（Claude Code skill），是 /interview 从「静态题库输出」升级为「多轮对话式模拟面试+评分反馈」的最短路径；其行为面与领域题的分类、评分 rubric 设计可直接并入现有 skills/interview-coach/SKILL.md 的流程，不引入任何云依赖。
- 融合方式：直接集成
- ⚠️ 限制：规模较小（347★），license 未在页面确认；集成时需按本系统铁律改造——凡涉及「经历类」回答仍必须锚定 evidence_index，其默认允许自由发挥的部分要收紧。

#### 🟢 karanb192/algo-sensei
- 地址：https://github.com/karanb192/algo-sensei（⭐277）｜Markdown/Python（Claude Code Skill + MCP）
- 核心能力：Claude Code 专用的 LeetCode/DSA 导师技能：诊断用户水平→校准难度→引导式刷题（不给答案、逐步启发）→复盘讲解→按遗忘曲线安排复习，另有限时竞赛模式。
- 引入理由：秋招算法笔试/手撕环节是应届生最大短板，而本系统 /upskill 只做知识缺口清单、无训练闭环。algo-sensei 的「诊断-校准-引导-复习」机制可直接嫁接为 /upskill 的算法子流程，把技能缺口真正变成每日可执行的刷题计划，全程 Claude Code 原生运行、零外部服务。
- 融合方式：直接集成
- ⚠️ 限制：2025-10 后更新放缓；license 未确认；题源是 LeetCode 英文站，国内牛客/华为机考题型需自行补充。

#### 🔵 JWM0203/MeetingCopilot
- 地址：https://github.com/JWM0203/MeetingCopilot（⭐255）｜TypeScript（Windows 桌面应用）
- 核心能力：Windows 实时会议/面试副驾：流式 ASR（本地 FunASR 完全离线或云）+ 第一人称提词器 + 屏幕共享隐形悬浮窗，支持 Ollama 本地大模型，中英混合场景优化，Apache-2.0。
- 引入理由：线上面试场景是本系统完全缺失的一环。它的价值在于给出一条「全本地」的实时链路参考（FunASR 流式识别→LLM 生成第一人称回答→低延迟提词 UI），与本系统「一切本地、数据不出机」的约束完全一致；练习模式下可改成「模拟面试官说话→用户作答→系统评分」的口语陪练。
- 融合方式：参考思路
- ⚠️ 限制：仅支持 Windows；依赖用户本机音频捕获权限；用于真实面试的隐形提词涉及平台规则与诚信风险，本系统只应将其用于练习/复盘，仓库页面已确认 Apache-2.0。

#### 🔵 offercontext/offerPilot
- 地址：https://github.com/offercontext/offerPilot（⭐727）｜Python
- 核心能力：开源、本地优先的 AI 求职工作台：简历管理、刷题练习、模拟面试、面试复盘、Offer 对比与谈薪指导一体化，数据全部存本地（active，2026-09 有提交）。
- 引入理由：它是与本项目定位最接近的成熟整站，其「面试复盘」模块（面完记录被问了什么、答得如何、沉淀成个人高频题库）和「Offer/谈薪对比」正是本系统 /interview 与 /pipeline 之后缺失的两块；另外其本地优先的数据模型对统一 data/ 下的岗位-简历-面试记录 schema 有直接借鉴价值。
- 融合方式：参考思路
- ⚠️ 限制：AGPL-3.0（已在仓库页面确认）：强传染协议，禁止摘抄其代码进本系统后再对外分发，只借鉴数据模型与流程设计；功能面广，需裁剪，避免堆砌。

### 匹配算法

#### 🟢 google/langextract
- 地址：https://github.com/google/langextract（⭐38.6k）｜Python
- 核心能力：用LLM把非结构化文本抽成结构化数据，带源文字锚定（字符级offset+可视化）。定义few-shot样例即可适配任意领域，无需微调；README明确支持通过内置Ollama接口跑本地模型，'On-device models don't require an API key'（v1.7.0已验证）。
- 引入理由：直接解决'结构化JD解析'这个子问题：把Boss/猎聘抓来的原始JD文本稳定地抽成岗位名、技能清单、经验/学历要求、薪资范围等JSON，且每个字段都能回溯到JD原文（防幻觉，符合'事实不可捏造'铁律）。是这条搜索线上唯一兼具超大社区规模和真离线能力的方案。
- 融合方式：直接集成
- ⚠️ 限制：本地跑需自备Ollama+一个小模型（如qwen2.5:7b），抽取质量依赖few-shot样例质量；英文样例为主，中文JD需自写中文示例。

#### 🟢 BAAI/FlagEmbedding
- 地址：https://github.com/BAAI/FlagEmbedding（⭐未精确核实（知名项目，数千级；页面已确认含bge-m3、bge-reranker））｜Python
- 核心能力：BGE系列embedding与reranker官方库：bge-m3支持100+语言（含中文）、8k上下文、dense/sparse/multi-vec三路混合检索，纯本地推理；另有bge-reranker可做精排。
- 引入理由：中文'岗位↔简历'向量化匹配的最优离线底座：同一模型编码JD和简历到共享向量空间，余弦相似度即可做粗排，再用bge-reranker精排，全程不联网、中文效果显著好于英文系SBERT模型。MIT许可可随意内置。
- 融合方式：直接集成
- ⚠️ 限制：bge-base级别模型CPU可跑但偏慢，建议GPU或只用small档；需自行实现向量存储（numpy/faiss即可，不必引重型向量库）。

#### 🟢 ekzhu/datasketch
- 地址：https://github.com/ekzhu/datasketch（⭐未精确核实（API限流；PyPI确认v2.0.0近期发布、活跃维护））｜Python
- 核心能力：MinHash/LSH/LSH Forest/Weighted MinHash/LSH Ensemble/HNSW等概率数据结构库，对大规模文本做Jaccard近重复检测与相似检索，纯Python、无外部服务。
- 引入理由：多平台岗位聚合（Boss+猎聘+智联+实习僧）后的'去重算法'没有领域专用开源项目，datasketch就是事实标准：对 标题+公司+核心描述 分词shingle后用MinHashLSH，O(n)复杂度找出跨平台重复岗（同公司同岗位不同措辞、中介转发），比逐对精确比对快几个数量级。
- 融合方式：直接集成
- ⚠️ 限制：通用库，需自己定JD的shingle/分词策略和阈值（建议先用0.5~0.7调试）；对中文要先做字级n-gram或jieba分词。

#### 🟡 GresonKwan/JobOK
- 地址：https://github.com/GresonKwan/JobOK（⭐406）｜Markdown（纯Agent Skill，无代码）
- 核心能力：面向中文求职者的证据驱动求职Agent Skill（支持Claude Code/Codex）：真实经历→证据链→岗位信号→JD匹配（job-matches.csv，按证据/硬性条件/兴趣/现实约束/风险初筛）→简历版本→面试故事库→投递跟踪，缺证据内容标记needs_proof。
- 引入理由：唯一找到的中文+活跃+与Claude Code Skill形态完全对齐的求职匹配项目。它的'岗位匹配'评分字段设计（证据支撑、硬性门槛一票否决、现实约束、风险项）比通用英文项目的rubric更贴合国内校招语境，且其证据链+needs_proof机制正是提示词'事实不可捏造'原则的现成实现，可直接吸收进评分体系与双Agent审核。
- 融合方式：提取模块
- ⚠️ 限制：它是提示词工程而非算法实现，无embedding/去重代码；定位不自动投递、不爬平台，投递采集环节帮不上；MIT许可，提取时保留版权。

#### 🟡 nestauk/ojd_daps_skills
- 地址：https://github.com/nestauk/ojd_daps_skills（⭐162）｜Python
- 核心能力：英国Nesta的技能抽取库：从原始招聘广告文本抽取技能短语，并映射到任选技能分类（ESCO等），spaCy+transformer流水线，已在700万+英国岗位广告上验证，模型下载后完全离线。MIT。
- 引入理由：JD解析里最难的'从自由文本识别技能实体并归一到标准taxonomy'环节，它给了完整可复用的流水线与映射算法（含embedding匹配映射），比关键词表匹配鲁棒得多；其taxonomy映射思路可换成中文技能词表/国产分类体系复用。
- 融合方式：提取模块
- ⚠️ 限制：仅英文语料训练，对中文JD基本不可直接用，只能抽其映射框架自己重训/换词表；2025-06后更新放缓。

#### 🟡 julianrosenberger/CareerBERT
- 地址：https://github.com/julianrosenberger/careerbert（⭐16（论文代码仓库）；HF模型可直接下载）｜Python（HF模型仓库 julianrosenberger/CareerBERT，基于BAAI/bge-base-en-v1.5微调，MIT）
- 核心能力：ESWA 2025论文'CareerBERT: Matching resumes to ESCO jobs in a shared embedding space'官方实现：把简历与ESCO岗位编码进同一向量空间做通用岗位推荐，模型已在HuggingFace上发布（已核实存在、MIT）。
- 引入理由：提供了'简历-岗位双塔共享嵌入'的学术验证范式：一条向量点积就能得到简历↔岗位粗排分，作为五维评分里'技能匹配度'的客观分量；微调自bge-base说明用中文同源模型（bge系列）复刻这条路是通的，迁移方案现成。
- 融合方式：提取模块
- ⚠️ 限制：英文/ESCO体系语料训练，对中文简历-JD对效果未知，只宜借鉴架构并用BGE中文模型重训；仓库star少，社区支持有限。

#### 🟡 KonstantinosPetrakis/esco-skill-extractor
- 地址：https://github.com/KonstantinosPetrakis/esco-skill-extractor（⭐31）｜Python
- 核心能力：从JD/CV文本抽取ESCO技能并识别ISCO职业类别，基于sentence-transformers对ESCO技能库做语义检索匹配，已上PyPI（esco-skill-extractor），2025年仍活跃。
- 引入理由：比ojd_daps_skills更轻量、pip即装、模型本地推理的技能抽取参考实现：其'预计算技能库embedding+语义最近邻匹配'的技巧，可直接搬到中文场景——用本地中文embedding对自建中文技能词表做检索式抽取，替代昂贵的LLM逐条抽取，JD解析吞吐量大幅提升。
- 融合方式：提取模块
- ⚠️ 限制：ESCO分类不含中文，必须自建中文技能词表替换其库；star少属于个人项目，代码质量需自审。

### 海外/通用自动化

#### 🟢 JobSpy (python-jobspy)
- 地址：https://github.com/cullenwatson/JobSpy（⭐4.3k）｜Python
- 核心能力：一个 pip 库，一次调用并发抓取 LinkedIn、Indeed、Glassdoor、Google、ZipRecruiter、Bayt、Naukri、Bdjobs 八大岗位板，输出统一结构的岗位 DataFrame（SITE/TITLE/COMPANY/CITY/JOB_TYPE/薪资区间/JOB_URL/DESCRIPTION 等字段），支持代理、hours_old 新鲜度过滤、job_type/is_remote 筛选。Python>=3.10，MIT 协议。
- 引入理由：它就是'岗位采集→结构化清洗'这一段的现成实现：字段契约（统一列名的 DataFrame）可直接映射到本系统的统一岗位 JSON Schema，省去为每个海外平台硬写爬虫；salary 标准化（enforce_annual_salary）和新鲜度过滤正是校招过滤需要的能力。
- 融合方式：直接集成
- ⚠️ 限制：仅覆盖海外岗位板，对国内校招无效（本方向定位即海外备用路径）；反爬封锁真实存在——第三方评测结论是'works well until it gets blocked'，生产使用需配代理池；Indeed/Glassdoor 参数有互斥限制（README 已注明）。核验方式：raw.githubusercontent.com 实取 README+LICENSE（MIT，(c)2023 Cullen Watson），并用 PyPI project_urls 交叉确认本仓库为官方源（PyPI 包 python-jobspy v1.1.82 仍在更新）；star 数取自 shields.io 的 GitHub API 代理（2026-09-20）。

#### 🟡 JobSync
- 地址：https://github.com/Gsync/jobsync（⭐1.3k）｜Web 应用（Docker 自托管，语言栈未逐项核验）
- 核心能力：自托管求职管家：投递状态追踪+可视化看板、简历管理与 PDF 导出、AI 助手（简历评审/JD-简历匹配打分/写求职信/粘贴 JD 直接建岗）、公司监视与 job board API 定时拉新岗、面试题库、联系人管理、一键备份恢复。内置 MCP server：Claude 可直接在对话里加岗位、加面试题、存匹配分；AI 可经 Ollama 完全本地运行。
- 引入理由：这是所有已核验项目里与'运行在 Claude Code 中'的架构最同源的一个：它的 MCP 工具集设计（add_job / 存 match score / 加题库条目，且写入前需用户确认）可直接对照扩充本系统的 .mcp.json；其'投递记录+看板+备份恢复'的数据模型、'粘贴 JD 即入库'的降级入口，与 /pipeline 命令和数据契约设计一一对应。
- 融合方式：提取模块
- ⚠️ 限制：Docker 化 Web 应用而非库，不宜整体引入本地零依赖系统；自动化拉新岗对接的是海外 job board API（对国内平台无效）；star 基数较小（1.3k）。核验方式：实取 README（含 MCP 章节、Key Features、Quick Start）+ shields 确认 MIT 协议；非常活跃（上周一仍有提交）。

#### 🟢 Browser Use
- 地址：https://github.com/browser-use/browser-use（⭐115k）｜Python + TypeScript
- 核心能力：当前最主流的开源 AI 浏览器 agent：让 LLM 像人一样操作网页（导航、点选、填表、处理验证码），提供 Python/TypeScript 库与 CLI，内置 stealth 模式、验证码处理、住宅代理等反检测能力。
- 引入理由：它是 Selenium 类投递脚本（AIHawk、各 EasyApplyBot）的现代替代底座：任何'自动投递'环节（含 LinkedIn Easy Apply 或国内平台网页端）都可以由 LLM 驱动它按 JD 动态填表，而不是维护会过期的硬编码选择器——这直接对应本系统'投递执行层'与'平台适配器'的浏览器自动化方案选型。
- 融合方式：直接集成
- ⚠️ 限制：官网力推的云端浏览器/Agent API 是付费服务且违背'全部本地执行'约束——只用其开源库（pip 安装本地跑 Playwright）即可；作为 LLM agent 依赖需配 LLM key，依赖较重；用自动化投递仍需自担平台封号风险。核验方式：实取 README 与 LICENSE（MIT (c)2024 Gregor Zunic）；115k star、上周二仍有提交。

#### 🔵 LinkedIn-Easy-Apply-Bot
- 地址：https://github.com/nicolomantini/LinkedIn-Easy-Apply-Bot（⭐1.1k）｜Python (Selenium)
- 核心能力：经典 LinkedIn Easy Apply 自动投递机器人：配置 config.yaml（账号、目标职位/地点、经验与职位类型过滤）后自动登录、检索、逐岗打开并填表提交 Easy Apply，记录投递结果；作者有'睡觉时申请 1000 个岗位'的公开实战记录。
- 引入理由：它是'自动投递'环节的最小完整参考实现：搜索条件→岗位队列→逐岗表单问答→投递日志的循环结构、以及'哪些表单字段需要预置答案'的枚举，可直接映射为本系统投递风控与表单问答库的设计；对比 browser-use 也能看清'硬编码选择器 vs LLM 驱动'两条技术路线的取舍。
- 融合方式：参考思路
- ⚠️ 限制：Selenium 硬编码选择器，LinkedIn 前端一改即坏；LinkedIn 对机器人打击严格（DMCA 下架、封号风险真实存在，README 自述仅供教育用途）；2025 年 11 月后无提交，属半维护状态。仅借鉴管线设计，不建议直接运行。核验方式：实取 README + shields 确认 Apache-2.0。

#### 🔵 AIHawk（原 Auto_Jobs_Applier_AIHawk）及其管线遗产
- 地址：https://github.com/feder-cr/AIHawk（⭐新仓库 MIT；原投递项目历史约 30.3k star（2026-05-17 归档））｜Python
- 核心能力：本方向的源头项目。注意重大事实：著名的一键投递工具 Auto_Jobs_Applier_AIHawk / Jobs_Applier_AI_Agent_AIHawk 已被作者于 2026-05-17 归档转向商业产品，feder-cr 名下（及 AIHawk-app 等镜像名）现指向的是转型后的'反检测浏览器 + 编码代理 MCP server'（invisible-playwright，主打 undetected browsing / 无验证码）。原'岗位检索→AI定制简历→自动填表投递→投递追踪'完整管线代码现存的活跃副本是社区 fork，如 11844/Auto_Jobs_Applier_AIHawk。
- 引入理由：要借鉴的正是任务点名要提炼的那条管线：智能岗位检索（可配置搜索条件）、公司黑名单 companyBlacklist / 职位黑名单 titleBlacklist（对应本系统投递黑名单与外包/猎头岗过滤）、AI 雇主问答动态生成、每岗动态定制简历、投递记录追踪、plain_text_resume.yaml 结构化画像（与本系统统一用户画像 Schema 同构，可对照校验字段设计）。同时它转型后的 anti-detect 浏览器 + MCP 形态，代表了'平台反爬对抗'的当前答案，值得在投递执行层参考。
- 融合方式：参考思路
- ⚠️ 限制：原项目归档、不维护；fork 生态混乱：11844 fork 最后提交 2024-10 且已把协议从原 AGPL-3.0 改为'Proprietary License'，代码不可直接复制进本系统（协议污染），只可借鉴设计与管线；LinkedIn 官方对抗激烈，原项目当年即被媒体报道封号与信息失真风险（Business Insider/TechCrunch 报道链接仍在现 README 中）。另核实 AIHawk-ai/Auto_Jobs_Applier_AI_Agent（搜索中出现过的名字）实际不存在（shields 返回 repo not found），madingess/EasyApplyBot 存在但仅 175 star、2025-03 后停更，均不值列入清单。核验方式：实取 feder-cr/AIHawk README（确认已转型）、11844 fork README 与 LICENSE、旧地址 301 重定向链。

### 风控与看板

#### 🟢 lexiforest/curl_cffi
- 地址：https://github.com/lexiforest/curl_cffi（⭐6.5k (2026-09-20, shields.io)）｜Python (curl-impersonate C 绑定)
- 核心能力：requests 风格 API 的 HTTP 客户端（同步+asyncio），可模拟 Chrome/Edge/Safari/Firefox 的 TLS(JA3/JA4)、HTTP/2 帧指纹与请求头，带完整 cookie/proxy/Session 支持。
- 引入理由：Boss直聘/猎聘等国内平台在 API 层大量依赖 TLS 指纹识别 requests/httpx/aiohttp 的默认指纹并封号。直接把采集层 HTTP 客户端换成 curl_cffi 是投入产出比最高的风控对抗手段：改动小（接口几乎兼容 requests）、无需起浏览器、速度快，且持续跟进 Chrome 最新版本（最后提交 2026-09 当天，活跃度极高）。
- 融合方式：直接集成
- ⚠️ 限制：只解决客户端指纹层面；登录态 cookie（Boss直聘需扫码登录取 token）、滑块验证码、账号行为分析不在其覆盖范围内，需另行设计。impersonate 目标版本需定期升级以免指纹过旧。

#### 🟢 Kaliiiiiiiiii-Vinyzu/patchright
- 地址：https://github.com/Kaliiiiiiiiii-Vinyzu/patchright（⭐4.6k (2026-09-20, shields.io)）｜TypeScript/JavaScript + Python
- 核心能力：Playwright 的补丁版（drop-in 替换，只改 import），修补 Runtime.enable/Console.enable/命令行标志等自动化泄露点，支持 closed shadow root，README 声称可通过 Cloudflare/Kasada/Akamai/DataDome 等检测，仅支持 Chromium。
- 引入理由：直接回答了'playwright-stealth 之后的替代品'这个问题：原 AtuboDad/playwright-stealth 已年久失修，而 patchright 是 2026 年仍活跃（本月有提交）、Apache-2.0 的现代方案。若投递流程必须走 Chromium（如某些平台仅 Chrome 兼容），patchright 是首选浏览器自动化后端，迁移成本≈0。
- 融合方式：直接集成
- ⚠️ 限制：仅 Chromium（不支持 Firefox/WebKit）；README 明示仅供教育用途，自动化投递的合规风险需由系统层声明；版本需跟随上游 Playwright 升级。与 camoufox 二选一作为浏览器后端即可（Chromium 用 patchright，Firefox 用 camoufox）。

#### 🟢 daijro/browserforge
- 地址：https://github.com/daijro/browserforge（⭐1.2k (2026-09-20, shields.io)）｜Python
- 核心能力：基于 Apify fingerprint-suite 数据的浏览器请求头/指纹生成器：生成统计上真实、内部自洽的 User-Agent、sec-ch-ua、头顺序、视口/字体/平台组合（贝叶斯生成网络），可用 Playwright/Pyppeteer 注入。
- 引入理由：反爬检测不只看单项指纹，更看一致性（UA 说 Chrome 但 sec-ch-ua 缺失、视口与屏幕不符即露馅）。curl_cffi 只给 TLS 指纹，browserforge 补齐'每个本地账号会话绑定一套稳定自洽的浏览器身份'这一环，是 HTTP 采集层与浏览器层的公共指纹供应源。
- 融合方式：直接集成
- ⚠️ 限制：README 已将 Playwright 指纹注入功能标记为 deprecated（官方推荐浏览器层改用 Camoufox）——应只用其 header/UA/指纹数据生成能力，不做注入；更新频率为 2026-08，低于 camoufox。

#### 🟢 mjpieters/aiolimiter
- 地址：https://github.com/mjpieters/aiolimiter（⭐780 (2026-09-20, shields.io)）｜Python
- 核心能力：asyncio 漏桶算法限速器，AsyncLimiter(max_rate, time_period) 精确控制代码段的进入速率，配合 async with 一行实现节流。
- 引入理由：落地提示词中'单条消息间隔≥30秒、单平台限速'的现成构件：把投递动作包进 AsyncLimiter(1, 30) 即硬性保证节奏，从机制上防手抖超速，而不是靠自觉 sleep。体积极小（纯 Python 单包）、零依赖、维护极活跃（最后提交 2026-09 前一天），作为风控限速层的依赖无任何负担。
- 融合方式：直接集成
- ⚠️ 限制：仅 asyncio；限速状态不持久化——'每日≤25份'的日额度计数与'敏感时段'判断需由系统自己用本地 JSON/SQLite 实现并跨进程共享，aiolimiter 只负责速率本身。

#### 🟢 streamlit/streamlit
- 地址：https://github.com/streamlit/streamlit（⭐46k (2026-09-20, shields.io)）｜Python
- 核心能力：纯 Python 脚本快速生成交互式数据看板：st.metric 状态卡、st.bar_chart/plotly 图表、st.dataframe、st.data_editor 可编辑表格、st.cache_data、侧边栏筛选，默认只绑定 localhost。
- 引入理由：本地看板的第一选择：/pipeline 命令可以用 ~100 行 Python 生成投递漏斗（已投/已读/约面/offer）、评分分布、渠道转化、每日投递额度剩余等看板；st.data_editor 还能让用户在看板里直接改岗位状态，形成闭环。46k star、每日常更新、文档最全，Claude Code 生成与维护该文件的可靠性最高。
- 融合方式：直接集成
- ⚠️ 限制：本质是本地 web 服务（localhost:8501）而非纯静态文件，不是'零进程'方案；依赖较重（pyarrow 等）；若要求可邮件/离线分享的单文件报告，改用 pyecharts 出静态 HTML。

#### 🟢 pyecharts/pyecharts
- 地址：https://github.com/pyecharts/pyecharts（⭐16k (2026-09-20, shields.io)）｜Python
- 核心能力：Python 版 ECharts 封装（V2 基于 ECharts 5），30+ 图表类型、400+ 地图，一条 render() 输出自包含的交互式 HTML，支持 Page 组合多图成看板页与快照导出 PNG。
- 引入理由：真正零服务、零常驻进程的本地可视化路线：/pipeline 可以直接产出一份离线 HTML 看板报告（漏斗图+地图分布+趋势线），双击即看、不依赖任何运行中的服务，与'全部本地、禁止上传'的铁律一致；中文文档与中文社区最完善，图表在中文标签/国内地图上开箱即用。
- 融合方式：直接集成
- ⚠️ 限制：维护节奏低于 streamlit（最近提交 2026-02）；HTML 渲染依赖本地 echarts.min.js（需内嵌或离线存放，避免运行时请求 CDN 泄露访问记录）；导出 PNG 需要 snapshot 插件+浏览器驱动，可不做。

#### 🟢 simonw/datasette
- 地址：https://github.com/simonw/datasette（⭐11k (2026-09-20, shields.io)）｜Python
- 核心能力：SQLite/CSV 数据的零配置本地浏览与发布工具：datasette serve data.db 即得到可筛选/分面/全文搜索的 Web 界面与只读 JSON API，支持 canned queries 固化常用查询，默认仅监听 localhost。
- 引入理由：把统一数据契约（本地 JSON/SQLite 岗位库）直接变成可查询界面：/pipeline 明细查重、按公司黑名单核验'是否重复投递'、按渠道/状态切片，全部零代码获得，还能给其他模块提供只读 JSON API。与 streamlit 互补：streamlit 管图表看板，datasette 管明细数据的即席查询。
- 融合方式：直接集成
- ⚠️ 限制：定位是数据浏览/发布而非图表看板（图表能力弱，不替代前两者）；写操作需插件或经外部流程完成；启动后是常驻本地服务，需注意只绑 127.0.0.1 以符合本地数据原则。

## 三、采纳原则（宁缺毋滥的执行标准）

1. 全部70项均通过存在性与内容核验；没有核验过的项目哪怕star再高也不进清单；
2. 「直接集成」只给能作为依赖/数据源/MCP接入的；改几个文件的都算「提取模块」；
3. 平台方案可用性以『能否在本地有头环境跑通登录+采集+导出JSON』为唯一标准，本环境无法验证的一律降级并如实标注；
4. 协议合规：PolyForm Noncommercial（get_jobs）、无LICENSE（xixiluo95/zhaopin 等）的项目只作参考思路或子进程调用，不拷贝源码再分发。